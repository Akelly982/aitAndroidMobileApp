<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aidankelly.projectmanager">


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.SearchHomeActivity"/>
        <activity android:name=".activities.ChangeImageActivity" />
        <activity android:name=".activities.ChangeTextActivity" />
        <activity android:name=".activities.DeleteConfirmationActivity" />
        <activity android:name=".activities.ProjectAddItem" />
        <activity android:name=".activities.ProjectActivity" />
        <activity android:name=".activities.HomeEditActivity" />
        <activity android:name=".activities.NewProjectActivity" />
        <activity android:name=".activities.HomeActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MainActivity"/>
    </application>

</manifest>